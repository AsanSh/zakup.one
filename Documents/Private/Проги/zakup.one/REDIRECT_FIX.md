# 🔧 Исправление проблемы с перенаправлением

## ❌ Проблема
Система автоматически перенаправляла всех пользователей на `/app.html`, даже когда они хотели попасть на главную страницу `zakup.one`.

## ✅ Решение

### **1. Исправлена логика определения устройств**

#### **Старая логика (неправильная):**
```javascript
// Всегда перенаправляла на app.html
if (currentPath === '/' || currentPath === '/index.html') {
    window.location.href = '/web-version.html';
    return;
}
```

#### **Новая логика (правильная):**
```javascript
// Если на главной странице, остаемся там
if (currentPath === '/' || currentPath === '/index.html') {
    return; // Stay on main page
}

// Если на app.html но не в Telegram, перенаправляем на главную
if (currentPath.includes('app.html') && !this.isTelegram) {
    window.location.href = '/';
    return;
}
```

### **2. Убрано автоматическое перенаправление из главной страницы**

#### **Было:**
```html
<script>
    // Автоматическое перенаправление на веб-версию
    window.location.href = '/web-version.html';
</script>
```

#### **Стало:**
```html
<script>
    // Простая логика для переключения на Telegram App
    document.addEventListener('DOMContentLoaded', function() {
        // Если пользователь в Telegram, перенаправляем на app.html
        if (window.Telegram && window.Telegram.WebApp) {
            window.location.href = '/app.html';
        }
    });
</script>
```

### **3. Удален device-detection.js из главной страницы**

Главная страница больше не загружает сложную систему определения устройств, которая вызывала нежелательные перенаправления.

## 🎯 Новая логика работы

### **Главная страница (https://zakup.one/)**
- ✅ **Показывает полный функционал** - LiveSearch, корзина, заказы
- ✅ **Остается на месте** - никаких автоматических перенаправлений
- ✅ **Только для Telegram** - перенаправляет на `/app.html`

### **Telegram App (https://zakup.one/app.html)**
- ✅ **Только для Telegram** - мобильная версия
- ✅ **Если не в Telegram** - перенаправляет на главную страницу

### **Пользовательские сценарии:**

#### **Сценарий 1: Обычный пользователь**
1. Заходит на `https://zakup.one/`
2. Видит главную страницу с полным функционалом
3. Может искать товары, добавлять в корзину, оформлять заказы

#### **Сценарий 2: Telegram пользователь**
1. Открывает бота в Telegram
2. Автоматически перенаправляется на `/app.html`
3. Получает мобильную версию с Telegram API

#### **Сценарий 3: Прямая ссылка на app.html**
1. Заходит на `https://zakup.one/app.html` не из Telegram
2. Автоматически перенаправляется на главную страницу
3. Получает полный функционал

## 🚀 Результат

### **Что исправлено:**
- ✅ **zakup.one открывается** - главная страница работает
- ✅ **Нет лишних перенаправлений** - только когда нужно
- ✅ **Telegram App работает** - для пользователей Telegram
- ✅ **Простая логика** - понятно и предсказуемо

### **Что работает:**
- ✅ **Главная страница** - https://zakup.one/
- ✅ **LiveSearch** - поиск товаров
- ✅ **Корзина** - управление товарами
- ✅ **Заказы** - оформление заказов
- ✅ **Telegram App** - https://zakup.one/app.html

## 📱 Тестирование

### **Проверьте:**
1. **Откройте https://zakup.one/** - должна показаться главная страница
2. **Попробуйте поиск** - введите "арматура"
3. **Добавьте товары в корзину** - должно работать
4. **В Telegram** - должно перенаправлять на app.html

**Проблема решена! Главная страница теперь открывается и работает корректно.** 🎊
