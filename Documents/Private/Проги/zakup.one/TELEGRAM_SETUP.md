# Настройка Telegram бота для zakup.one

## Пошаговая инструкция

### 1. Создание бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Введите имя бота: `zakup.one`
4. Введите username бота: `zakup_one_bot` (должен заканчиваться на `_bot`)
5. Сохраните полученный токен бота

### 2. Настройка Web App

1. Отправьте @BotFather команду `/newapp`
2. Выберите вашего бота
3. Введите название приложения: `zakup.one`
4. Введите описание: `Заказ строительных материалов`
5. Загрузите иконку приложения (512x512px)
6. Введите URL приложения: `https://zakup.one`
7. Введите URL для кнопки "Открыть": `https://zakup.one`

### 3. Настройка команд бота

Отправьте @BotFather команду `/setcommands` и выберите вашего бота, затем введите:

```
start - 🏠 Запустить zakup.one
help - ❓ Помощь
catalog - 📦 Каталог товаров
cart - 🛒 Корзина
profile - 👤 Профиль
```

### 4. Настройка меню бота

Отправьте @BotFather команду `/setmenubutton` и выберите вашего бота, затем введите:

```
zakup.one - https://zakup.one
```

### 5. Настройка домена

Убедитесь, что домен `zakup.one` правильно настроен и SSL сертификат активен.

### 6. Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите на кнопку "zakup.one" или "Открыть"
4. Приложение должно открыться в Telegram

## Структура URL

- Основное приложение: `https://zakup.one`
- Альтернативный URL: `https://zakup.one/index.html`

## Возможные проблемы

### Приложение не открывается
- Проверьте, что URL доступен по HTTPS
- Убедитесь, что SSL сертификат действителен
- Проверьте, что файл `index.html` существует

### Ошибки в консоли
- Откройте DevTools в браузере
- Проверьте ошибки JavaScript
- Убедитесь, что Telegram WebApp API загружается

### Проблемы с темой
- Приложение автоматически адаптируется под тему Telegram
- Проверьте CSS переменные для темной/светлой темы

## Дополнительные настройки

### Уведомления
Для отправки уведомлений пользователям используйте Telegram Bot API:

```javascript
// Пример отправки уведомления
fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        chat_id: USER_CHAT_ID,
        text: 'Ваш заказ оформлен!'
    })
});
```

### Webhook (опционально)
Для получения обновлений от Telegram:

```javascript
// Установка webhook
fetch(`https://api.telegram.org/bot${BOT_TOKEN}/setWebhook`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        url: 'https://zakup.one/webhook'
    })
});
```

## Безопасность

1. Никогда не публикуйте токен бота в коде
2. Используйте HTTPS для всех запросов
3. Валидируйте все входящие данные
4. Ограничьте доступ к административным функциям

## Мониторинг

- Используйте Telegram Bot API для получения статистики
- Настройте логирование ошибок
- Мониторьте производительность приложения
