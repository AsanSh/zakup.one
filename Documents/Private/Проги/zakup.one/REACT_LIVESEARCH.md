# 🧩 LiveSearch с корзиной и оформлением заказа - zakup.one

## 📋 Обзор

React-версия zakup.one с продвинутым поиском товаров, корзиной и системой оформления заказов.

### ✨ Функции:
- 🔍 **LiveSearch** - поиск с первой буквы
- 🛒 **Динамическая корзина** - добавление/удаление товаров
- 📦 **Оформление заказов** - форма с валидацией
- 🏷️ **Фильтрация по категориям** - быстрый поиск
- 💰 **Подсчёт суммы** - автоматический расчёт
- 📱 **Telegram WebApp** - интеграция с Telegram
- 🔄 **API Fallback** - работает без сервера

## 🚀 Быстрый старт

### 1. **Веб-версия (только фронтенд)**
```bash
# Откройте в браузере
open react-version.html
```

### 2. **Полная версия с бэкендом**

#### Установка зависимостей:
```bash
npm install
```

#### Запуск сервера:
```bash
npm start
# или для разработки
npm run dev
```

#### Открытие приложения:
```
http://localhost:3000
```

## 📁 Структура файлов

```
zakup.one/
├── react-version.html      # React приложение
├── react-styles.css        # Стили для React версии
├── api.js                  # Локальный API (fallback)
├── backend.js              # Node.js сервер
├── package.json            # Зависимости
├── demo-data.js            # Демо данные
└── REACT_LIVESEARCH.md     # Эта инструкция
```

## 🔧 API Endpoints

### **Поиск товаров**
```http
GET /api/products?search=арматура&category=Арматура
```

### **Создание заказа**
```http
POST /api/orders
Content-Type: application/json

{
  "customer": {
    "name": "Иван Иванов",
    "phone": "+996555123456",
    "address": "ул. Ленина, 1"
  },
  "items": [
    {
      "id": 1,
      "name": "Арматура А500С Ø8",
      "price": 120,
      "unit": "м"
    }
  ],
  "total": 120
}
```

### **Получение категорий**
```http
GET /api/categories
```

### **Проверка здоровья**
```http
GET /api/health
```

## 🎯 Компоненты

### **LiveSearch Component**
```jsx
function LiveSearch() {
  const [query, setQuery] = useState("");
  const [results, setResults] = useState([]);
  const [cart, setCart] = useState([]);
  const [category, setCategory] = useState("all");
  const [showCheckout, setShowCheckout] = useState(false);
  const [customer, setCustomer] = useState({ 
    name: "", phone: "", address: "" 
  });
}
```

### **Функции поиска**
- **Автопоиск** - с задержкой 300мс
- **Фильтрация** - по названию, поставщику, категории
- **Категории** - динамический список
- **Fallback** - локальный API при недоступности сервера

### **Корзина**
- **Добавление/удаление** - чекбоксы в таблице
- **Подсчёт суммы** - автоматический
- **Валидация** - проверка полей формы
- **Сохранение** - в localStorage при отсутствии API

## 🎨 Стили и дизайн

### **Адаптивный дизайн**
- 📱 **Мобильные устройства** - оптимизированная таблица
- 💻 **Компьютеры** - полный функционал
- 🎨 **Тёмная тема** - автоматическое определение

### **Анимации**
- **Fade In** - плавное появление
- **Hover Effects** - интерактивные элементы
- **Loading States** - состояния загрузки

## 🔄 Интеграция с существующей системой

### **Связь с zakup.one**
- **Единая база данных** - общие товары
- **Синхронизация** - между версиями
- **Переключатели** - между мобильной и React версией

### **Telegram WebApp**
- **API интеграция** - полная поддержка
- **Уведомления** - через Telegram
- **Автоопределение** - устройства

## 📊 Данные и состояние

### **Состояние приложения**
```javascript
const [query, setQuery] = useState("");           // Поисковый запрос
const [results, setResults] = useState([]);      // Результаты поиска
const [cart, setCart] = useState([]);             // Корзина
const [category, setCategory] = useState("all");  // Выбранная категория
const [showCheckout, setShowCheckout] = useState(false); // Форма заказа
const [customer, setCustomer] = useState({...});  // Данные клиента
```

### **Локальное хранение**
```javascript
// Заказы
localStorage.setItem('zakup-orders', JSON.stringify(orders));

// Предпочтения
localStorage.setItem('zakup-preferred-version', 'react');
```

## 🚀 Развёртывание

### **Статический хостинг**
```bash
# Загрузите файлы на сервер
scp react-version.html user@server:/var/www/html/
scp react-styles.css user@server:/var/www/html/
scp api.js user@server:/var/www/html/
```

### **С сервером**
```bash
# На сервере
npm install
npm start

# Настройте nginx для проксирования
location /api {
    proxy_pass http://localhost:3000;
}
```

## 🔧 Настройка

### **API URL**
```javascript
// В react-version.html измените URL
const apiUrl = 'https://your-domain.com/api/products';
```

### **Категории**
```javascript
// Обновите список категорий
const categories = ["Все", "Арматура", "Двутавры", ...];
```

### **Стили**
```css
/* Настройте цвета в react-styles.css */
:root {
  --primary-color: #2a5298;
  --secondary-color: #1e3c72;
}
```

## 📈 Производительность

### **Оптимизации**
- **Debounced Search** - задержка 300мс
- **Lazy Loading** - загрузка по требованию
- **Caching** - кэширование результатов
- **Fallback** - работа без сервера

### **Метрики**
- **Время поиска** - < 300мс
- **Размер бандла** - ~50KB
- **Совместимость** - все современные браузеры

## 🎯 Готово к использованию!

### ✅ **Что работает:**
- 🔍 **LiveSearch** - поиск товаров в реальном времени
- 🛒 **Корзина** - добавление/удаление товаров
- 📦 **Заказы** - полный цикл оформления
- 🏷️ **Фильтры** - по категориям и поставщикам
- 📱 **Адаптивность** - для всех устройств
- 🔄 **API** - с fallback на локальные данные

### 🚀 **Запуск:**
1. **Быстрый старт**: Откройте `react-version.html`
2. **С сервером**: `npm install && npm start`
3. **В продакшене**: Загрузите файлы на сервер

**React версия с LiveSearch полностью готова к использованию!** 🎊
